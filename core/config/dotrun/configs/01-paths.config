# DotRun Configuration - PATH
#
# File naming convention: NN-category.config
# - NN: Two-digit number (01, 02, etc.) for load order
# - category: Descriptive name (paths, api, dev, etc.)
# - extension: .config (required)
#
# Files are loaded in alphanumeric order. Use numbers to ensure
# dependencies load first (e.g., PATH before tools that use it).

# Add ~/.local/bin to PATH if not already present
case ":$PATH:" in
  *":$HOME/.local/bin:"*) ;;
  *) export PATH="$HOME/.local/bin:$PATH" ;;
esac

export DR_HELPERS_VERBOSE=0
export DR_HELPERS_QUIET=1
