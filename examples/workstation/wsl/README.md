# wsl — list Cloud Workstation status

### Synopsis

`wsl`

### Description

Lists all workstations in the configured cluster and prints the status column
for the workstation defined in `helpers/constants.sh`.

### Example

```bash
$ drun wsl
RUNNING
```
